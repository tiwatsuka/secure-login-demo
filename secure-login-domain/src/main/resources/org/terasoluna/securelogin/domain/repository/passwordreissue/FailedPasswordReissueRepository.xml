<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="org.terasoluna.securelogin.domain.repository.passwordreissue.FailedPasswordReissueRepository">

	<resultMap id="failedPasswordReissueResultMap" type="FailedPasswordReissue">
		<id property="token" column="token" />
		<id property="attemptDate" column="attempt_date" />
	</resultMap>

	<select id="findByToken" resultMap="failedPasswordReissueResultMap">
    <![CDATA[
        SELECT
            token,
            attempt_date
        FROM
            failed_password_reissue
        WHERE
            token = #{token}
        ORDER BY attempt_date DESC
    ]]>
	</select>

	<insert id="insert" parameterType="FailedPasswordReissue">
    <![CDATA[
        INSERT INTO failed_password_reissue (
            token,
            attempt_date
        ) VALUES (
	        #{token},
            #{attemptDate}
        )
    ]]>
	</insert>

	<delete id="deleteByToken">
    <![CDATA[
    	DELETE FROM
    		failed_password_reissue
    	WHERE
    		token = #{token}
    ]]>
	</delete>

	<delete id="deleteExpired">
    <![CDATA[
    	DELETE FROM
    		failed_password_reissue
    	WHERE
    		token = 
    		(SELECT
    			token
    		 FROM
    		 	password_reissue_info
    		 WHERE
    		 	token = failed_password_reissue.token 
    		 AND
    			expiry_date < #{date}
    		)
    ]]>
	</delete>
</mapper>